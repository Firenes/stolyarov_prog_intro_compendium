#!/bin/sh

I=10
MYFILE=./variables.sh
MYSTRING="Hello, World!"

# Print values in variables
#echo $I $MYFILE $MYSTRING

# Print value in variables in string
#echo "${I} niggers"

# Print arithmetic operation with variable
# Typed. Arithmetic operations must carry out with numeric
#echo "I=$(( $I + 7 ))"

# Also can write "I=$((I+7))"


# I variable doesn't change after echo operation
#echo "Now I is ${I}"
