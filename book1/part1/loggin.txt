Протоколирование (логирование) происходит с помощью команды script имя_файла.txt

Протоколирование будет записывать в файл все команды и распечатки, которые пользователь будет совершать.

Чтобы завершить процесс, то нужно нажать Ctrl-D ("конец файла")
